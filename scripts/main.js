(function() {
  var generateSystem;

  generateSystem = function(systemOptions) {
    return 0;
  };

  $(document).ready(function() {
    return $("#options").submit(function(e) {
      var systemOptions;
      systemOptions = {};
      return false;
    });
  });

}).call(this);
